import { makeStyles } from "@material-ui/core/styles"

export const useStyles = makeStyles({
    form: {
        "& *": {
            marginTop: 5,
        },
    },
    button: {
        width: 100,
    },
})
